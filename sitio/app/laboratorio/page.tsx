"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import { useState } from "react";
const steps=["Describe una necesidad observable sin nombrar tecnología.","Identifica la única autoridad capaz de aceptar cambios.","Declara identidad, ámbito, revisión y versión del documento.","Escribe el contrato feliz y dos rechazos.","Mide la alternativa más simple antes de crear una proyección.","Define reconstrucción, publicación, frescura y retiro.","Prueba repetición, revisión obsoleta y cruce de ámbito.","Conserva configuración, resultado, amenaza y responsable."];
export default function Page(){const [done,setDone]=useState<boolean[]>(steps.map(()=>false));const total=done.filter(Boolean).length;return <main id="contenido"><header className="nav"><a className="brand" href="/">COBRIA <span>laboratorio</span></a><nav><a href="/">Inicio</a><a href="/explorar">Buscar</a><a href="/mapa">Mapa</a><a href="/recursos">Recursos</a></nav></header><div className="page-shell"><header><p className="eyebrow">Práctica guiada</p><h1>Diseña una decisión verificable.</h1><p className="lead">Marca cada paso cuando puedas mostrar su evidencia. El progreso vive solo en esta página y no constituye conformidad ni certificación.</p></header><p className="search-count" aria-live="polite">{total} de {steps.length} pasos completados</p><section className="lab-list">{steps.map((x,i)=><label className="lab-step" key={x}><input type="checkbox" checked={done[i]} onChange={()=>setDone(v=>v.map((b,j)=>j===i?!b:b))}/><span><b>Paso {i+1}</b><br/>{x}</span></label>)}</section></div></main>}
